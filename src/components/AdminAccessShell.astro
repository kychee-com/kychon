---
import AdminAccessShellView from './kychon/AdminAccessShellView';

const {
  title = 'Checking access',
  body = 'Confirming your admin session.',
} = Astro.props;
---

<AdminAccessShellView title={title} body={body} />
